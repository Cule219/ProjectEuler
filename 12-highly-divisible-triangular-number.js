/**
    The sequence of triangle numbers is generated by adding the natural numbers. So the 7th triangle number would be 1 + 2 + 3 + 4 + 5 + 6 + 7 = 28. The first ten terms would be:
    1, 3, 6, 10, 15, 21, 28, 36, 45, 55, ...
    Let us list the factors of the first seven triangle numbers:
        1: 1import data from "~/Documents/primer-dataset.json";
        3: 1,3
        6: 1,2,3,6import data from "~/Documents/primer-dataset.json";
        10: 1,2,5,10
        15: 1,3,5,15
        21: 1,3,7,21
        28: 1,2,4,7,14,28
    We can see that 28 is the first triangle number to have over five divisors.
    What is the value of the first triangle number to have over five hundred divisors?
 */

function myFunc() {
  let seq = 0;
  let count = 1;

  while (countDivisors(seq) <= 501) {
    seq += count;
    count++;
  }
  return [seq, count];
}

// let odd = [];

// let num = 1,
//   i = 1;
// while (countDivisors(n) < 5) {
//   console.log(n);
//   n += n + 1;
//   i += 1;
// }
// // while (odd.length <= 10) {
// //     console.log(countDivisors(num, odd));
// //     num++;
// // }

// console.log(i, odd);

function countDivisors(num) {
  let i = 2;
  let count = 0;
  while (i <= num / 2) {
    if (num % i === 0) count++;
    i++;
  }
  return count;
}

// let res = myFunc();
// console.log(res);
